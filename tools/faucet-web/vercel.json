export const config = { api: { bodyParser: false } }

export default {
  buildCommand: 'npm run build',
  framework: 'nextjs',
  functions: {
    'src/app/api/**/*.ts': {
      runtime: 'nodejs20.x',
      maxDuration: 30
    }
  }
}
