# Protocol freeze tip for image build (refresh after every security/ops merge).
# docker build must check out this commit (or a release tag pointing here).
# Verify: docker run --rm $IMAGE xrpld --version
#
# branch: develop
# includes: Critical slash/claim/bridge + High latency/CI + ops ceremony pack
c323951e2611bbf03102d50c542b95b736c8ffeb
